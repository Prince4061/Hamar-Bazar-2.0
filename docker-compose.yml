version: '3.8'

services:
  web:
    build: .
    container_name: hamar-bazar-web
    ports:
      - "5001:5001"
    volumes:
      - hamar_bazar_data:/data
      - hamar_bazar_uploads:/app/static/uploads
    environment:
      - DATABASE_PATH=/data/marketplace.db
      - FLASK_SECRET_KEY=hyperlocal_monopolistic_secret_key_12345
    restart: always

volumes:
  hamar_bazar_data:
  hamar_bazar_uploads:

